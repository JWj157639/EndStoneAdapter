# WsConnectionManager 实现计划

## 一、项目概述

### 1.1 目标
创建一个 WebSocket 连接状态管理器，解决当前项目中 WebSocket 连接状态管理混乱的问题。

### 1.2 核心特性
- 基于 Supabase Realtime 状态管理理念
- 原子操作防止重复重连
- 统一状态处理入口
- 梯度延迟重连策略
- 断线数据补全机制

### 1.3 文件结构
```
D:\esp32\EndStoneAdapter\include\quiz\
├── WsConnectionManager.h          # 头文件
├── WsConnectionManager.cpp        # 源文件
├── 计划.md                        # 本文档
└── websocketfiles/               # WebSocket 库（已有）
    ├── src/
    │   ├── ws_endpoint.h          # WebSocketEndpoint 类
    │   ├── ws_packet.h            # WebSocketPacket 类
    │   └── ...
    └── include/
        └── uv/                    # libuv 头文件（可选）
```

### 1.4 技术方案
- **协议层**：使用 websocketfiles 的 WebSocketEndpoint 和 WebSocketPacket
- **网络层**：复用 WebSocketClient.cpp 的 TCP 连接代码（原生 socket）
- **状态管理**：实现原子状态机，防止重复重连
- **异步调度**：使用 EndStone 的 Scheduler 实现异步延迟重连

## 二、架构设计

### 2.1 分层架构

```
┌─────────────────────────────────┐
│     应用层（对外接口）           │
│  - initConnect()                │
│  - closeConnect()               │
│  - autoReconnect()               │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   状态管理层（核心逻辑）          │
│  - handleStatusChange()         │
│  - autoReconnect()               │
│  - syncLoseMessage()             │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   协议层（websocketfiles）       │
│  - 继承 WebSocketEndpoint       │
│  - 重写 user_defined_process()   │
│  - 使用 WebSocketPacket         │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   网络层（复用 WebSocketClient） │
│  - hostname_connect()           │
│  - TCP socket 管理              │
│  - send/recv 原生 socket        │
└─────────────────────────────────┘
```

### 2.2 类设计

#### WsConnectionManager 类
```cpp
class WsConnectionManager : public WebSocketEndpoint {
public:
    WsConnectionManager(endstone::Logger* logger, endstone::Scheduler* scheduler);

    void initConnect();
    void closeConnect();
    void autoReconnect();

    void OnStatusChange(std::function<void(ConnectionStatus, std::string)> callback);
    void OnMessageReceived(std::function<void(std::string)> callback);

    void SendText(const std::string& text);

private:
    enum class ConnectionStatus {
        SUBSCRIBED,
        CHANNEL_ERROR,
        TIMED_OUT,
        CLOSED
    };

    static const int RECONNECT_FAST_DELAY = 1000;
    static const int RECONNECT_SLOW_DELAY = 3000;
    static const int TIMEOUT_SECONDS = 30;

    std::atomic<bool> isConnecting;

    endstone::Logger* logger;
    endstone::Scheduler* scheduler;
    int reconnectCount;
    int maxReconnectTimes;
    std::string lastMessageId;
    ConnectionStatus currentStatus;

    std::chrono::system_clock::time_point lastActivityTime;
    std::shared_ptr<endstone::Task> reconnectTask;
    std::shared_ptr<endstone::Task> timeoutCheckTask;

    std::function<void(ConnectionStatus, std::string)> statusChangeCallback;
    std::function<void(std::string)> messageReceivedCallback;

    void handleStatusChange(ConnectionStatus status, const std::string& errorMsg = "");
    void syncLoseMessage();
    void showConnectTip();
    void showErrorTip(const std::string& errorMsg);

    int32_t user_defined_process(WebSocketPacket& packet, ByteBuffer& frame_payload) override;
    void startTimeoutCheck();
    void stopTimeoutCheck();
    std::string generateMessageId();
    std::string generateMd5(const std::string& input);

    void networkRecvLoop();
    bool performHandshake(const std::string& hostname, int port, const std::string& path);
    void networkSend(const char* data, int64_t size);

    socket_t wsSocket;
    std::thread recvLoopThread;
    std::mutex sendMutex;
    std::string serverUrl;
};
```

## 三、核心逻辑实现

### 3.1 连接状态枚举

```cpp
enum class ConnectionStatus {
    SUBSCRIBED,
    CHANNEL_ERROR,
    TIMED_OUT,
    CLOSED
};
```

### 3.2 原子状态机核心逻辑

**连接/重连前检查：**
```cpp
void initConnect() {
    bool expected = false;
    if (!isConnecting.compare_exchange_strong(expected, true)) {
        logger->info("已有连接正在进行，跳过本次连接请求");
        return;
    }

    try {

    } catch (...) {
        isConnecting = false;
    }
}
```

**状态变更处理：**
```cpp
void handleStatusChange(ConnectionStatus status, const std::string& errorMsg) {
    currentStatus = status;

    switch (status) {
        case ConnectionStatus::SUBSCRIBED:
            reconnectCount = 0;
            isConnecting = false;
            syncLoseMessage();
            startTimeoutCheck();
            if (statusChangeCallback) {
                statusChangeCallback(status, "");
            }
            break;

        case ConnectionStatus::CHANNEL_ERROR:
        case ConnectionStatus::TIMED_OUT:
            isConnecting = false;
            stopTimeoutCheck();
            autoReconnect();
            break;

        case ConnectionStatus::CLOSED:
            isConnecting = false;
            stopTimeoutCheck();
            if (reconnectTask) {
                reconnectTask->cancel();
                reconnectTask = nullptr;
            }
            break;
    }
}
```

### 3.3 重连策略

```cpp
void autoReconnect() {
    if (reconnectCount >= maxReconnectTimes) {
        showErrorTip("无法恢复连接，请刷新页面");
        return;
    }

    int delay = (reconnectCount < 3) ? RECONNECT_FAST_DELAY : RECONNECT_SLOW_DELAY;

    logger->info("将在 {} 毫秒后进行第 {}/{} 次重连",
                 delay, reconnectCount + 1, maxReconnectTimes);

    reconnectCount++;

    reconnectTask = scheduler->runDelayed([this]() {
        initConnect();
    }, std::chrono::milliseconds(delay));
}
```

### 3.4 超时检测

```cpp
void startTimeoutCheck() {
    timeoutCheckTask = scheduler->runTask([this]() {
        if (currentStatus == ConnectionStatus::SUBSCRIBED) {
            auto now = std::chrono::system_clock::now();
            auto elapsed = std::chrono::duration_cast<std::chrono::seconds>(
                now - lastActivityTime
            ).count();

            if (elapsed >= TIMEOUT_SECONDS) {
                logger->warning("连接超时，最后活动时间: {} 秒前", elapsed);
                handleStatusChange(ConnectionStatus::TIMED_OUT, "连接超时");
            }
        }
    }, std::chrono::seconds(5));
}

void stopTimeoutCheck() {
    if (timeoutCheckTask) {
        timeoutCheckTask->cancel();
        timeoutCheckTask = nullptr;
    }
}
```

### 3.5 消息 ID 生成

```cpp
std::string generateMessageId() {
    auto now = std::chrono::system_clock::now();
    auto timestamp = std::chrono::duration_cast<std::chrono::milliseconds>(
        now.time_since_epoch()
    ).count();

    std::string input = std::to_string(timestamp) + "_" +
                       std::to_string(rand());

    return generateMd5(input);
}

std::string generateMd5(const std::string& input) {
    unsigned char digest[MD5_DIGEST_LENGTH];
    MD5((unsigned char*)input.c_str(), input.size(), digest);

    char md5String[33];
    for (int i = 0; i < 16; i++) {
        sprintf(&md5String[i * 2], "%02x", (unsigned int)digest[i]);
    }
    md5String[32] = '\0';

    return std::string(md5String);
}
```

### 3.6 断线数据补全

```cpp
void syncLoseMessage() {
    std::string newMessageId = generateMessageId();

    if (!lastMessageId.empty()) {
        logger->info("同步断线期间丢失的消息，从 {} 到 {}",
                     lastMessageId, newMessageId);

        if (statusChangeCallback) {
            statusChangeCallback(ConnectionStatus::SUBSCRIBED,
                               "需要同步数据: " + lastMessageId);
        }
    }

    lastMessageId = newMessageId;
}
```

### 3.7 WebSocket 适配层

```cpp
int32_t WsConnectionManager::user_defined_process(
    WebSocketPacket& packet,
    ByteBuffer& frame_payload
) {
    lastActivityTime = std::chrono::system_clock::now();

    switch (packet.get_opcode()) {
        case WebSocketPacket::WSOpcode_Text:
        case WebSocketPacket::WSOpcode_Binary:
            std::string msg(frame_payload.bytes(), frame_payload.length());

            if (messageReceivedCallback) {
                messageReceivedCallback(msg);
            }

            if (currentStatus != ConnectionStatus::SUBSCRIBED) {
                handleStatusChange(ConnectionStatus::SUBSCRIBED);
            }
            break;

        case WebSocketPacket::WSOpcode_Close:
            handleStatusChange(ConnectionStatus::CLOSED, "服务端主动关闭连接");
            break;

        case WebSocketPacket::WSOpcode_Ping:
            {
                WebSocketPacket pongPacket;
                pongPacket.set_fin(1);
                pongPacket.set_opcode(WebSocketPacket::WSOpcode_Pong);
                pongPacket.set_payload(frame_payload.bytes(), frame_payload.length());

                ByteBuffer output;
                pongPacket.pack_dataframe(output);
                networkSend(output.bytes(), output.length());
            }
            break;

        default:
            break;
    }

    return 0;
}
```

### 3.8 网络层实现

```cpp
void WsConnectionManager::networkRecvLoop() {
    std::vector<char> buffer;
    buffer.reserve(8192);

    while (currentStatus == ConnectionStatus::SUBSCRIBED) {
        struct timeval tv;
        tv.tv_sec = 0;
        tv.tv_usec = 200 * 1000;

        fd_set fds_read;
        FD_ZERO(&fds_read);
        FD_SET(wsSocket, &fds_read);
        int ret = select((int)(wsSocket + 1), &fds_read, NULL, NULL, &tv);

        if (ret < 0) {
            handleStatusChange(ConnectionStatus::CHANNEL_ERROR, "socket select error");
            break;
        }

        if (ret == 0) {
            continue;
        }

        if (FD_ISSET(wsSocket, &fds_read)) {
            std::array<char, 4096> buf = {};
            int bytesReceived = recv(wsSocket, buf.data(), (int)buf.size(), 0);
            if (bytesReceived > 0) {
                from_wire(buf.data(), bytesReceived);
            } else {
                handleStatusChange(ConnectionStatus::CHANNEL_ERROR, "connection lost");
                break;
            }
        }
    }
}

bool WsConnectionManager::performHandshake(const std::string& hostname, int port, const std::string& path) {
    std::string handshakingWords;
    handshakingWords.append("GET ").append(path).append(" HTTP/1.1").append("\r\n");
    handshakingWords.append("Host: ").append(hostname).append(":").append(std::to_string(port)).append("\r\n");
    handshakingWords.append("Connection: Upgrade").append("\r\n");
    handshakingWords.append("Upgrade: websocket").append("\r\n");
    handshakingWords.append("Sec-WebSocket-Version: 13").append("\r\n");
    handshakingWords.append("Sec-WebSocket-Key: O7Tk4xI04v+X91cuvefLSQ==").append("\r\n");
    handshakingWords.append("\r\n");

    int sendResult = send(wsSocket, handshakingWords.c_str(), (int)handshakingWords.size(), 0);
    if (sendResult == SOCKET_ERROR) {
        logger->error("握手发送失败");
        return false;
    }

    char response_buffer[4096] = { 0 };
    int bytesReceived = recv(wsSocket, response_buffer, 4096, 0);
    if (bytesReceived <= 0) {
        logger->error("握手响应失败");
        return false;
    }

    std::string response(response_buffer, bytesReceived);
    if (response.find("101 Switching Protocols") == std::string::npos) {
        logger->error("握手失败: {}", response);
        return false;
    }

    return true;
}

void WsConnectionManager::networkSend(const char* data, int64_t size) {
    std::lock_guard<std::mutex> lock(sendMutex);
    int sendResult = send(wsSocket, data, (int)size, 0);
    if (sendResult == SOCKET_ERROR) {
        throw std::runtime_error("socket send error");
    }
}

void WsConnectionManager::SendText(const std::string& text) {
    if (currentStatus != ConnectionStatus::SUBSCRIBED) {
        throw std::runtime_error("WebSocket is not subscribed");
    }

    WebSocketPacket packet;
    packet.set_fin(1);
    packet.set_opcode(WebSocketPacket::WSOpcode_Text);
    packet.set_payload(text.c_str(), text.size());

    ByteBuffer output;
    packet.pack_dataframe(output);
    networkSend(output.bytes(), output.length());
}
```

## 四、配置管理

### 4.1 配置项扩展

```cpp
int ConfigManager::GetMaxReconnectTimes() const {
    return data_.value("maxReconnectTimes", 10);
}
```

### 4.2 配置文件示例

```json
{
  "version": 3,
  "serverId": "...",
  "hashKey": "...",
  "maxReconnectTimes": 10,
  "chatFormat": {
    "from_game": "<{name}> {msg}",
    "from_group": "群:<{nick}> {msg}",
    "post_chat": true,
    "post_prefix": ""
  },
  "motdUrl": "play.easecation.net:19132",
  "serverName": "EndStone"
}
```

## 五、使用示例

### 5.1 初始化连接管理器

```cpp
#include "quiz/WsConnectionManager.h"

class BotClient {
private:
    WsConnectionManager* wsManager;

public:
    BotClient(endstone::Logger* logger) {
        wsManager = new WsConnectionManager(logger);

        wsManager->OnStatusChange([this](ConnectionStatus status, std::string msg) {
            this->onStatusChanged(status, msg);
        });

        wsManager->OnMessageReceived([this](std::string msg) {
            this->onTextMsg(msg);
        });
    }

    void connect() {
        wsManager->initConnect();
    }

    void onStatusChanged(ConnectionStatus status, std::string msg) {
        switch (status) {
            case ConnectionStatus::SUBSCRIBED:
                logger->info("连接已建立");
                break;
            case ConnectionStatus::CHANNEL_ERROR:
                logger->error("连接错误: {}", msg);
                break;
            case ConnectionStatus::TIMED_OUT:
                logger->warning("连接超时");
                break;
            case ConnectionStatus::CLOSED:
                logger->info("连接已关闭");
                break;
        }
    }
};
```

## 六、测试计划

### 6.1 单元测试
- [ ] 测试原子变量防止重复连接
- [ ] 测试状态转换逻辑
- [ ] 测试重连次数限制
- [ ] 测试梯度延迟策略
- [ ] 测试超时检测机制
- [ ] 测试消息 ID 生成唯一性

### 6.2 集成测试
- [ ] 测试与 WebSocketEndpoint 的集成
- [ ] 测试网络异常场景
- [ ] 测试服务端主动断开
- [ ] 测试断线重连流程
- [ ] 测试数据补全机制

### 6.3 压力测试
- [ ] 测试频繁断线重连
- [ ] 测试高并发场景
- [ ] 测试长时间运行稳定性

## 七、注意事项

### 7.1 线程安全
- 使用 `std::atomic` 保护关键变量
- 使用互斥锁保护共享资源
- 避免死锁和竞态条件

### 7.2 资源管理
- 及时释放网络资源
- 正确处理线程生命周期
- 避免内存泄漏

### 7.3 错误处理
- 捕获所有异常
- 记录详细错误日志
- 提供友好的错误提示

### 7.4 兼容性
- 不修改 websocketfiles 模块外的代码
- 保持与现有代码的兼容性
- 遵循项目命名规范

## 八、实现步骤

### 阶段一：基础框架
1. 创建 WsConnectionManager.h 头文件
2. 定义类结构和成员变量
3. 实现构造函数和析构函数

### 阶段二：核心逻辑
1. 实现状态枚举和状态机
2. 实现原子变量保护机制
3. 实现 handleStatusChange() 方法
4. 实现 autoReconnect() 方法

### 阶段三：适配层
1. 继承 WebSocketEndpoint
2. 重写 user_defined_process() 方法
3. 实现超时检测机制
4. 实现消息 ID 生成

### 阶段四：配置集成
1. 扩展 ConfigManager
2. 读取配置参数
3. 实现配置热重载

### 阶段五：测试和优化
1. 编写单元测试
2. 进行集成测试
3. 性能优化
4. 文档完善

## 九、风险评估

### 9.1 技术风险
- **风险**：WebSocketEndpoint 的回调机制可能不够灵活
- **应对**：通过适配器模式封装，提供更友好的接口

### 9.2 性能风险
- **风险**：超时检测线程可能影响性能
- **应对**：使用条件变量减少轮询开销

### 9.3 兼容性风险
- **风险**：与现有代码集成可能出现冲突
- **应对**：保持接口简洁，提供清晰的迁移指南

## 十、总结

本实现计划基于 Supabase Realtime 状态管理理念，采用原子操作防止重复重连，统一状态处理入口，梯度延迟重连策略，以及断线数据补全机制。通过分层架构设计，将应用层、状态管理层和适配层分离，提高代码的可维护性和可扩展性。

遵循项目命名规范，不修改模块外代码，确保与现有代码的兼容性。通过完善的测试计划和风险评估，确保代码质量和稳定性。